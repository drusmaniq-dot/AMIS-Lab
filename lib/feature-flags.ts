// Temporary: member self-service for Projects/Publications is disabled while
// the member dashboard is simplified to Profile + Services only. Flip this
// back to true to restore it for everyone — the pages/actions behind it are
// unchanged, just gated on this flag (admins always bypass it).
export const MEMBER_CONTENT_SUBMISSIONS_ENABLED = false;
